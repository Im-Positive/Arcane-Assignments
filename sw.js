self.addEventListener('push',event=>{let data={};try{data=event.data?.json()||{}}catch{}const title=typeof data.title==='string'?data.title:'Arcane Assignments';const body=typeof data.body==='string'?data.body:'A quest needs your attention.';event.waitUntil(self.registration.showNotification(title,{body,icon:'/icon.svg',badge:'/icon.svg',tag:typeof data.tag==='string'?data.tag:'arcane-reminder',data:{url:'/'}}))});self.addEventListener('notificationclick',event=>{event.notification.close();event.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(windows=>{const existing=windows.find(w=>new URL(w.url).origin===self.location.origin);return existing?existing.focus():clients.openWindow('/')}))});
