export const questTypes=Object.freeze({homework:{label:'Homework',power:20,weight:1},reading:{label:'Reading',power:15,weight:1},quiz:{label:'Quiz',power:30,weight:2},project:{label:'Project',power:50,weight:3},exam:{label:'Exam',power:60,weight:4}});
export function typeOf(value){return Object.hasOwn(questTypes,value)?value:'homework'}
export function isAssessment(value){return value==='quiz'||value==='exam'}
export function status(item,now){if(item.done)return 'done';if(item.due<now)return 'overdue';if(item.due-now<=48*60*60*1000)return 'soon';return 'later'}
export function score(item){if(!item.done)return 0;const base=questTypes[typeOf(item.type)].power;if(isAssessment(typeOf(item.type))){return Number.isFinite(item.grade)?Math.round(base*Math.max(0,Math.min(100,item.grade))/100):0}return item.completedAt!=null&&item.completedAt<=item.due?base:Math.max(5,Math.round(base*.4))}
export function stability(pressure){return Math.max(0,Math.round(100-15*Math.max(0,pressure)))}
export function summary(items,now){const open=items.filter(x=>!x.done),soon=open.filter(x=>status(x,now)==='soon'),overdue=open.filter(x=>status(x,now)==='overdue'),xp=items.reduce((n,x)=>n+score(x),0),pressure=soon.reduce((n,x)=>n+questTypes[typeOf(x.type)].weight,0)+overdue.reduce((n,x)=>n+questTypes[typeOf(x.type)].weight*2,0),stable=stability(pressure);return {open:open.length,soon:soon.length,overdue:overdue.length,completed:items.length-open.length,xp,pressure,stability:stable,level:Math.floor(xp/100)+1,progress:xp%100,mood:stable<=35?'volatile':stable<100?'uneasy':'calm'}}
export function duePreset(kind,now=new Date()){const date=new Date(now);if(kind==='today'){date.setHours(23,59,0,0)}else if(kind==='tomorrow'){date.setDate(date.getDate()+1);date.setHours(17,0,0,0)}else if(kind==='week'){date.setDate(date.getDate()+7);date.setHours(17,0,0,0)}else if(kind==='weekend'){let days=(6-date.getDay()+7)%7;if(days===0&&date.getHours()>=18)days=7;date.setDate(date.getDate()+days);date.setHours(18,0,0,0)}else if(kind==='yesterday'){date.setDate(date.getDate()-1);date.setHours(17,0,0,0)}else throw Error('Unknown preset');return date}
